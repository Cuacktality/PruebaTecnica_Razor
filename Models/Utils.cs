namespace PruebaTecnica.Models {
    public enum MyRole { None, Seeker, Employer }
    public enum MyStatus { None, Sended, Viewed, Interview, OnTest, Completed, Rejected }
    public enum EducationLevel { None, Tech, Technologist, Professional, Postgraduate }
    public enum JobMode { None, Remote, OnSite }
    public enum Payment { None, Mensual, Weekly }
}
